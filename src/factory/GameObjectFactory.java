/*
package factory;
import Main.GamePanel;
import object.OBJ_Pokeball;
import object.SuperObject;

public class GameObjectFactory implements ObjectFactory{
    private final GamePanel gp;

    public GameObjectFactory(GamePanel gp) {
        this.gp = gp;
    }

    @Override
    public SuperObject createPokeball() {
        return new OBJ_Pokeball(gp);
    }

    @Override
    public SuperObject createPotion() {
        // Assumed constructor, replace with actual if different
        return null;
    }
}
*/
